diff -Naur orig/libsystemd.la patched/libsystemd.la
--- orig/libsystemd.la	1970-01-01 01:00:00.000000000 +0100
+++ patched/libsystemd.la	2017-12-18 00:34:16.977321374 +0100
@@ -0,0 +1,41 @@
+# libsystemd.la - a libtool library file
+# Generated by libtool (GNU libtool) 2.4.6
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# The name that we can dlopen(3).
+dlname='libsystemd.so.@libsystemd_version_major@'
+
+# Names of this library.
+library_names='libsystemd.so.@libsystemd_version@ libsystemd.so.@libsystemd_version_major@ libsystemd.so'
+
+# The name of the static archive.
+old_library=''
+
+# Linker flags that cannot go in dependency_libs.
+inherited_linker_flags=' -pthread'
+
+# Libraries that this one depends upon.
+dependency_libs=' -lresolv -lcap -lrt /usr/lib/liblzma.la /usr/lib/libgcrypt.la /usr/lib/libgpg-error.la'
+
+# Names of additional weak libraries provided by this library
+weak_library_names=''
+
+# Version information for libsystemd.
+current=19
+age=19
+revision=0
+
+# Is this an already installed library?
+installed=yes
+
+# Should we warn about portability when linking against -modules?
+shouldnotlink=no
+
+# Files to dlopen/dlpreopen
+dlopen=''
+dlpreopen=''
+
+# Directory that this library needs to be installed in:
+libdir='/usr/lib'
diff -Naur orig/libudev.la patched/libudev.la
--- orig/libudev.la	1970-01-01 01:00:00.000000000 +0100
+++ patched/libudev.la	2017-12-18 00:37:13.588019088 +0100
@@ -0,0 +1,41 @@
+# libudev.la - a libtool library file
+# Generated by libtool (GNU libtool) 2.4.6
+#
+# Please DO NOT delete this file!
+# It is necessary for linking the library.
+
+# The name that we can dlopen(3).
+dlname='libudev.so.@libudev_version_major@'
+
+# Names of this library.
+library_names='libudev.so.@libudev_version@ libudev.so.@libudev_version_major@ libudev.so'
+
+# The name of the static archive.
+old_library=''
+
+# Linker flags that cannot go in dependency_libs.
+inherited_linker_flags=' -pthread'
+
+# Libraries that this one depends upon.
+dependency_libs=' -lresolv -lcap -lrt'
+
+# Names of additional weak libraries provided by this library
+weak_library_names=''
+
+# Version information for libudev.
+current=7
+age=6
+revision=6
+
+# Is this an already installed library?
+installed=yes
+
+# Should we warn about portability when linking against -modules?
+shouldnotlink=no
+
+# Files to dlopen/dlpreopen
+dlopen=''
+dlpreopen=''
+
+# Directory that this library needs to be installed in:
+libdir='/usr/lib'
